export declare const appConfig: (() => {
    port: number;
    nodeEnv: string;
    apiVersion: string;
    allowedOrigins: string[];
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    port: number;
    nodeEnv: string;
    apiVersion: string;
    allowedOrigins: string[];
}>;
